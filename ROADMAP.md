# نقشه راه · Ba Man Bebin Roadmap

## ✅ فاز ۱ — رفع باگ و بهینه‌سازی موبایل

- [x] آیکون پلی برعکس → `border-left` به‌جای `border-inline-start` (مثلث پلی همیشه به راست اشاره می‌کند)
- [x] overlay دکمه پلی وسط صفحه (`play-gate`) با CSS triangle و حداقل ۴۸×۴۸px
- [x] `showPlayGate()` بعد از `applySource()` برای حالت autoplay blocked
- [x] بهینه‌سازی موبایل: کنترل‌ها جمع‌وجور، دکمه‌ها ≥۴۴px، چت ارتفاع مناسب
- [x] حرکات لمسی MX Player: روشنایی (نیمه چپ)، بلندی صدا (نیمه راست)، seek (افقی)، دو ضربه ±۱۰s
- [x] فوتر: ساخته شده با ❤️ · BA MAN BEBIN 2026
- [x] صفحه وضعیت گرافیکی `/status` با حلقه SVG، uptime، CPU/RAM، نسخه Node/ffmpeg/yt-dlp
- [x] endpoint `/api/system` با `os.cpus()`, `os.totalmem()`, `process.uptime()`

---

## ✅ فاز ۲ — تماس تصویری/صوتی + کم شدن هوشمند صدا

**تماس تصویری/صوتی با WebRTC (مش)**
- [x] ورودی/خروجی صدا و تصویر با `getUserMedia()` و `RTCPeerConnection`
- [x] Signalling نقطه‌به‌نقطه روی Socket.IO موجود (`call:offer`, `call:answer`, `call:ice-candidate`)
- [x] مش تا **۴ نفر** (۶ اتصال). بالای ۴ نفر هشدار — با ۵ نفر هر کاربر ۲٫۴Mbps آپلود لازم دارد که روی موبایل ایران لنگ می‌زند
- [x] جلوگیری از glare: «فقط کسی که socket ID کوچک‌تری دارد offer می‌فرستد» — با ۴ نفر دقیقاً ۶ اتصال بدون تداخل
- [x] پاک‌سازی کامل موقع خروج: `peer.close()` + حذف از map + توقف تراک‌ها
- [x] یک ICE restart خودکار برای جابه‌جایی Wi-Fi ↔ ۴G
- [x] جلوگیری از اکو: `echoCancellation` + `noiseSuppression` + `autoGainControl`

**TURN اختیاری (مهم برای ایران)**
- [x] کد با STUN تنها کار می‌کند (۸۵-۹۰٪ موارد)
- [x] `TURN_URL` / `TURN_USER` / `TURN_PASS` از `.env` (چندتایی با کاما) — بدون تغییر کد اضافه می‌شوند
- [x] endpoint `/api/ice` تا کلیدها در سورس کلاینت دیده نشوند
- [x] هشدار «اتصال مستقیم ممکن نشد — TURN لازم است» بعد از ۱۰ ثانیه در حالت connecting
- [x] مستندات `turns:` روی پورت ۴۴۳ + Cloudflare Realtime TURN و Metered.ca در README

**کم شدن هوشمند صدای فیلم (Web Audio API — نه AI)**
- [x] میکروفون (و صدای همه اعضای تماس) → `AnalyserNode` → RMS → `GainNode` روی صدای فیلم
- [x] اعداد آزمایش‌شده: نمونه‌برداری ۵۰ms · آستانه RMS ۰٫۰۶ · کف ۰٫۲۵ · نگه‌داشتن ۸۰۰ms · ثابت زمانی ۰٫۱۵
- [x] واکنش در ۱۵۰ میلی‌ثانیه؛ مکث کوتاه وسط حرف صدا را بالا نمی‌پراند
- [x] سه مسیر خروجی: `GainNode` · `element.volume` (cross-origin) · `setVolume()` (یوتیوب)
- [x] کاربر می‌تواند حساسیت و شدت کم‌شدن را تنظیم کند (ذخیره در `localStorage`)
- [x] بدون هیچ مدل AI — روی سرور ۲ هسته‌ای فقط تأخیر می‌سازد

**رابط کاربری**
- [x] حالت عادی: نوار کوچک و شیک تصاویر در گوشه پلیر، قابل جمع شدن
- [x] تمام‌صفحه: PiP قابل جابه‌جایی با کشیدن، ۱۶:۹ یا دایره‌ای کوچک (دابل‌کلیک)
- [x] دکمه ✕: تماس تصویری ↔ صوتی بدون قطع شدن تماس (فقط `replaceTrack`)
- [x] کنترل‌ها: قطع میکروفون، قطع دوربین، خروج از تماس
- [x] موبایل: دکمه‌های ≥۴۴px، پاد باریک‌تر، پنل ducking شناور

**تست**
- [x] ۵۰ تست خودکار (`npm test`) بدون نیاز به مرورگر

---

## 🔜 فاز ۳ — زیرنویس و دوبله

- بارگذاری فایل SRT/VTT/ASS و sync با playhead
- استخراج زیرنویس از YouTube API (`yt-dlp --write-sub`)
- نمایش زیرنویس روی `<video>` با WebVTT (`<track>` یا overlay)
- پشتیبانی از چند زبان زیرنویس همزمان
- دوبله/نظرات فرعی: پخش فایل صوتی جداگانه همگام با فیلم (مثل نریشن فارسی روی فیلم انگلیسی)
- endpoint `/api/subtitles` برای بارگذاری و تبدیل فرمت‌ها

---

## 🔜 فاز ۴ — پیش‌نمایش و اسکرین‌شات و تاریخچه

- پیش‌نمایش اتاق: thumbnail ویدیو + تعداد اعضا قبل از ورود
- اسکرین‌شات از پلیر → `canvas.drawImage(video)` → ذخیره و اشتراک‌گذاری
- تاریخچه اتاق‌ها: لاگ ورود/خروج، تغییر منبع، seek events (ephemeral)
- iframe embed: `<iframe src="/embed/:id">` برای وبلاگ‌ها و سایت‌ها
- Share API: `navigator.share()` برای موبایل

---

## 🔜 فاز ۵ — دکمه‌های Deploy، ربات تلگرام و گوگل درایو

- دکمه Deploy یک‌کلیکی: Railway / Render / Fly.io / Vercel
- ربات تلگرام: ساخت اتاق + دریافت لینک دعوت + وضعیت سرور + اعلان‌ها
- گوگل درایو: ویدیوهای Drive رو مستقیم در اتاق پخش کن (`/api/media/drive?url=`)
- QR code لینک دعوت در اتاق
- PWA manifest + Service Worker برای offline و installable
